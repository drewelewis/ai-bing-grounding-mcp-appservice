# Azure OpenAI Configuration (for Semantic Kernel agent)
AZURE_OPENAI_ENDPOINT=https://your-openai-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-azure-openai-api-key
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o

# APIM MCP Server Configuration
# Azure API Management natively exposes REST APIs as MCP servers
# The MCP endpoint URL format: https://<apim-service-name>.azure-api.net/<api-name>-mcp/mcp
# Example: https://apim-abc123.azure-api.net/bing-grounding-mcp/mcp
APIM_MCP_SERVER_URL=https://your-apim-service.azure-api.net/your-api-mcp/mcp

# APIM Subscription Key (if required by your APIM instance)
# This is passed as the "Ocp-Apim-Subscription-Key" header
APIM_SUBSCRIPTION_KEY=your-apim-subscription-key

# Note: The test script uses:
# - Azure OpenAI for the Semantic Kernel agent (reasoning and orchestration)
# - APIM MCP server for tool access (web search with Bing Grounding and citations)
# - APIM automatically converts the REST API operations into MCP tools
# - The MCP protocol (HTTP/SSE transport) abstracts away backend implementation
